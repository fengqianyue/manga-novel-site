# ==================== Docker 环境专用配置 ====================
# 启动时通过 SPRING_PROFILES_ACTIVE=docker 激活

spring:
  datasource:
    url: jdbc:mysql://mysql:3306/manga_novel?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 123456

  # Docker 环境启用 Redis（如果不需要可保留 exclude）
  # autoconfigure:
  #   exclude:
  #     - org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration

app:
  es:
    uri: http://elasticsearch:9200
  upload-path: /app/uploads/
